{
  "name": "compliance-backend-server",
  "version": "1.0.0",
  "description": "Express server to connect Firebase backend to React frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Backend build complete'",
    "netlify:dev": "netlify dev",
    "netlify:build": "cd frontend && npm run build",
    "netlify:deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "openai": "^4.20.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@railway/cli": "^4.6.3",
    "netlify-cli": "^23.4.2",
    "nodemon": "^3.0.1",
    "render-cli": "^0.3.2"
  }
}
